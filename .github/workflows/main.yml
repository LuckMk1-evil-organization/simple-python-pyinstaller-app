on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    name: on tente des trucs
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      - name: build
        id: build
        uses: LuckMk1-evil-organization/simple-python-pyinstaller-app@1656ce33ca69daf405f64b00b8f9c5880b264b29
