on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    name: on tente des trucs
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      - name: build
        id: build
        uses: LuckMk1-evil-organization/simple-python-pyinstaller-app@8ec0b88f459922af578a4ac8539ec010a20500a7
