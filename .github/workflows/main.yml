on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    name: on tente des trucs
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      - name: build
        id: build
        uses: LuckMk1-evil-organization/simple-python-pyinstaller-app@c8251cf8da0102ea8db4f079c2b20ded4a70bac1
