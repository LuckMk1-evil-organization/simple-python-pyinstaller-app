on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    name: on tente des trucs
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      - name: build
        id: build
        uses: LuckMk1-evil-organization/simple-python-pyinstaller-app@b57e080d302af0bbe44a4ca1efeb7b42e4c06520
